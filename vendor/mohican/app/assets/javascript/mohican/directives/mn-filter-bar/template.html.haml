.row
  .col-xs-12.btn-toolbar
    %button.btn(type="button"
                ng-click="isBackendFilterPanelOpened = !isBackendFilterPanelOpened"
                ng-class="{'btn-primary': isBackendFilterPanelOpened}")
      Change working dataset
    %button.btn(type="button"
                ng-click="filterBar.reload()")
      Reload working dataset
    .btn(button-label="show" helper-elements=""
         input-model="filterBar.owner.layouts" isteven-multi-select=""
         item-label="name desc" ng-show="filterBar.owner.layouts.length > 1"
         on-item-click="filterBar.changeLayout(data.name)"
         selection-mode="single" tick-property="selected")
    %button.btn(ng-class="{'btn-primary': filterBar.owner.stateMachine.quickFilterShown}"
                ng-click="filterBar.owner.toggleQuickFilter()"
                ng-disabled="!filterBar.owner.fullyLoaded"
                type="button")
      %span.pull-left Quick Filter
      %span.loader.pull-left(ng-show="!filterBar.owner.fullyLoaded"
                             style="margin: 0px 0px 0px 8px;")
    %a.btn.btn-primary.pull-right(ng-if="filterBar.showNewButton === 'true'" href="#/{{filterBar.owner.resourceName}}/new") New
%div(ng-show="isBackendFilterPanelOpened")
  .well.well-lg
    .row
      .col-xs-12.btn-toolbar
        %form.form-horizontal
          %div.mn-translude
          .form-group
            .col-sm-offset-2.col-sm-10
              %button.btn.btn-default(ng-click="filterBar.changeDocumentFilter()") Apply Filter
