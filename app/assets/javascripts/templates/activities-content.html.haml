%span
  %strong(ng-if="!page_count") Loading data, please wait
%table.table.table-striped(ng-if="page_count")
  %thead
    %tr
      %th(ng-repeat="field_info in layout()" ng-style="{width: field_info.ColumnWidth + 'px'}") {{field_info.HeaderText}}
  %tr(ng-repeat="item in page_items")
    %td(ng-repeat="field_info in layout()") {{item.copy[field_info.Name]}}
